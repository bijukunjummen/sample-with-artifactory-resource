---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: ubuntu
    # tag: '8-alpine'

inputs:
  - name: repo
  - name: pipeline
outputs:
  - name: build
run:
  path: /bin/bash
  args: 
    - -c
    - |
        cd repo
        ./gradlew clean build
params:
  JAVA_OPTIONS: 
